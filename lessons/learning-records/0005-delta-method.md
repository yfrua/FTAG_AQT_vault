# Delta method: extrapolation of the tightest-bin light SF

The user knows the two-stage delta method (DoFit fixes SF_Neg_TagBin6_l = 1 as "alpha"; DoResults reports SF(tightest) := SF(adjacent) with all uncertainties inherited), the tagger-specific wiring (Continuous 6-bin, Continuous2D alpha/beta), and its open gap: only the light SF is protected, the b SF still floats in that bin.

**Evidence:** requested the lesson after connecting the SF_b runaway to the missing b-side protection.

**Implications:** ready for a hands-on exercise (e.g. verifying a delta-method reconstruction in DoResults, or the b-SF fix decision workflow).
